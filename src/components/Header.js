import React from "react";
import styled from "styled-components";

const Header = styled.header`
  display: block;
`;

export default () => (
  <Header>
    <h1 style={{ marginTop: 0 }}>APEX Icons</h1>
  </Header>
);
